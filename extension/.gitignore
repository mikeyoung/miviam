# Built bundles are generated by build.ps1 from the live web-app assets.
# They duplicate ~10 MB of mp3s per browser, so they are not committed — run
# `pwsh extension/build.ps1` to regenerate them.
dist/
*.zip
*.xpi
